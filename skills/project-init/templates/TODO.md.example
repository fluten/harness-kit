# TODO.md

<!-- 区块权限声明
| 区块 | 类型 | 说明 |
|------|------|------|
| 📊 快速状态 | 🔓 生成区 | AI 自动维护，按底部「重建算法」整体重写 |
| 当前任务 | 🔓 生成区 | Claude Code 自主维护 |
| 待办队列 | 🔓 生成区 | Claude Code 自主维护 |
| 已完成 | 🔓 生成区 | Claude Code 自主维护 |
| 规则 | 🔒 锁定区 | 每个项目一致，不可更改 |
-->

<!-- 🔓 START 生成区：📊 快速状态 -->
<!-- 🤖 AI 自动维护：修改本文件 body 任一区块后，必须按底部「🔒 规则 → 📊 快速状态 重建算法」整体重写本块 -->
## 📊 快速状态

- **进行中**: `2` 项
- **待办**: 高 `5` / 普通 `6` / 低 `4`
- **下一步**: `初始化前端项目（React + TypeScript + Vite + Tailwind）`
- **更新**: `2026-04-08`
<!-- 🔓 END 生成区：📊 快速状态 -->

<!-- 🔓 START 生成区：当前任务 -->
## 当前任务
- [ ] 初始化前端项目（React + TypeScript + Vite + Tailwind）
- [ ] 初始化后端项目（FastAPI + SQLAlchemy + Alembic）
<!-- 🔓 END 生成区：当前任务 -->

<!-- 🔓 START 生成区：待办队列 -->
## 待办队列

### 高优先级
- [ ] 创建数据库 schema 并运行 migration
- [ ] 生成模拟对话数据（CSV，含情感/意图标签）
- [ ] 实现用户注册/登录 API（JWT）
- [ ] 实现 CSV 上传导入 API
- [ ] 搭建看板页面框架（布局 + 空状态）

### 普通优先级
- [ ] 实现对话分析 API（调用 LLM）
- [ ] 看板：情绪趋势折线图（Recharts）
- [ ] 看板：高频问题 Top 10 柱状图
- [ ] 看板：质量分布饼图
- [ ] 对话列表页 + 单条对话详情页
- [ ] 看板数据筛选（时间范围、意图类型）

### 低优先级 / Nice to have
- [ ] 客服排名表
- [ ] 深色模式
- [ ] 报告生成 PDF 导出
- [ ] API 文档（Swagger UI 美化）
<!-- 🔓 END 生成区：待办队列 -->

<!-- 🔓 START 生成区：已完成 -->
## 已完成
<!-- 格式：- [x] {{任务描述}} ✅ {{日期}} {{关联编号（如有）}} -->
<!-- 🔓 END 生成区：已完成 -->

<!-- 🔒 START 锁定区：规则 -->
## 规则
1. 「当前任务」始终保持 1-3 项，做完一个再从「待办队列」拉一个上来
2. 完成任务时：
   - 标记 [x]
   - 移入「已完成」区，附上日期
   - 同步更新 PROGRESS.md
   - 如关联 issue，同步更新 ISSUES.md 状态
3. 新增任务时判断优先级放入对应区
4. 引用 issue 格式：`修复 BUG-AUTH-001`
5. 「已完成」区按时间倒序，最新的在最上面
6. **修改本文件 body 后，必须按下方「📊 快速状态 重建算法」整体重写顶部「📊 快速状态」区块**

### 📊 快速状态 重建算法

修改本文件 body（当前任务 / 待办队列 / 已完成）任一区块后，必须按以下步骤**整体重写**顶部「📊 快速状态」区块（禁止增量修改字段）。

**输入**：当前 TODO.md 的 body 部分
**输出**：4 行的「📊 快速状态」块

**步骤**：
1. `N_active` = 「## 当前任务」区下未完成 `[ ]` 项的数量
2. `N_high` = 「### 高优先级」区下 `[ ]` 项的数量
3. `N_normal` = 「### 普通优先级」区下 `[ ]` 项的数量
4. `N_low` = 「### 低优先级」或「### 低优先级 / Nice to have」区下 `[ ]` 项的数量
5. `NEXT` 取值规则：
   - 若「## 当前任务」非空 → `NEXT` = 首项 `[ ]` 的描述文本（去掉 `- [ ]` 前缀）
   - 否则若「### 高优先级」非空 → `NEXT` = 首项 `[ ]` 的描述文本
   - 否则 `NEXT` = `（无）`
6. `DATE` = 今天的日期（`YYYY-MM-DD` 绝对格式）

**填入顶部「📊 快速状态」区块**（按以下模板原样输出，将 `{var}` 替换为算出的值）：

````markdown
- **进行中**: `{N_active}` 项
- **待办**: 高 `{N_high}` / 普通 `{N_normal}` / 低 `{N_low}`
- **下一步**: `{NEXT}`
- **更新**: `{DATE}`
````
<!-- 🔒 END 锁定区：规则 -->
